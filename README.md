
  # Create website

  This is a code bundle for Create website. The original project is available at https://www.figma.com/design/pgReseIge6l27GSX8vAmQU/Create-website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  